# nodrop=1   - don't discard corrupt frames; a dropped frame mid-call is worse
#              than a briefly glitched one
# timeout    - the Brio is slow to answer control requests over SuperSpeed;
#              the default 3000ms is marginal and contributes to the
#              "Failed to set UVC probe control" errors
options uvcvideo nodrop=1 timeout=5000
