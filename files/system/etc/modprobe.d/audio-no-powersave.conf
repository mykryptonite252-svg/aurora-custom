# snd_hda_intel power_save defaults to 10 seconds on this hardware, which
# causes an audible pop/click when the codec wakes — very noticeable at the
# start of every call. Disable it; the power cost on a desktop-class laptop
# is negligible.
options snd_hda_intel power_save=0 power_save_controller=N
