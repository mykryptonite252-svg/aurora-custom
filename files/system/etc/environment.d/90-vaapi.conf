# Force the modern Intel media driver (iHD) rather than the legacy i965.
# This is what puts video decode AND encode on the iGPU instead of the CPU —
# the webcam upload path in a video call. Without it the CPU encodes every
# frame, which drives up heat and fan noise.
LIBVA_DRIVER_NAME=iHD
